# P0 主线函数级任务划分

## Summary

按需求文档固定的数据流开发，只做 P0：`原始 ToF 读数 → ZoneState → SystemState → FeedbackDecision → 三路震动输出`。成员分工沿用文档责任线：A 负责感知，B 负责反馈与电源佩戴，C 负责决策、状态调试与主循环集成。函数名可以直接采用下表，团队也可统一改名，但职责和输入输出不要混。

## 模块与函数设计

### 1. 公共配置与数据契约

负责人：C 初版建立，全员共同维护。

- `config.h`
  - 集中定义引脚：I2C 使用 `IO8/IO9`，左/中/右电机使用 `IO4/IO5/IO6`，LED 使用 `IO7`。
  - 集中定义 TCA9548A 地址、左/中/右通道映射、测距周期、日志周期、量程、滤波窗口、风险阈值、50mm 滞回、120mm 大范围障碍差值。
- 公共类型
  - `enum Direction { LEFT, CENTER, RIGHT }`
  - `enum RiskLevel { SAFE, NOTICE, WARN, DANGER }`
  - `struct ZoneState { direction, distanceMm, valid, lastValidMs, risk }`
  - `struct SystemState { zones[3], anySensorInvalid, allSensorsInvalid }`
  - `struct FeedbackDecision { activeDirections, risk, isWideObstacle }`
- 辅助函数
  - `directionName(Direction direction) -> const char*`：统一串口输出“左/中/右”。
  - `riskName(RiskLevel risk) -> const char*`：统一日志里的风险名称。
  - `isActiveRisk(RiskLevel risk) -> bool`：判断是否需要震动。

### 2. 感知模块

负责人：A。

目标：稳定输出左、中、右三个方向的毫米级有效距离，不让异常传感器拖死主循环。

| 函数 | 输入 | 输出 / 行为 |
|---|---|---|
| `perceptionInit()` | 无 | 初始化 I2C、TCA9548A、三路 VL53L1X，返回或记录初始化结果。 |
| `muxSelectChannel(uint8_t channel)` | TCA9548A 通道号 | 选通指定 ToF 通道，失败返回 `false`。 |
| `readRawDistanceMm(Direction direction)` | 方向 | 读取该方向原始距离；无数据、超时、读取失败返回无效结果。 |
| `isDistanceValid(uint16_t distanceMm)` | 原始距离 | 仅接受 `30-3000mm`，其他读数剔除。 |
| `filterPush(Direction direction, uint16_t distanceMm, uint32_t nowMs)` | 方向、有效距离、时间戳 | 每方向保留最近 5 个有效样本。 |
| `filterMedian(Direction direction)` | 方向 | 返回当前中值距离。 |
| `classifyInstantRisk(uint16_t distanceMm)` | 滤波距离 | 输出无滞回的即时风险档，用于感知侧诊断。 |
| `perceptionUpdate(uint32_t nowMs)` | 当前系统时间 | 约每 45ms 轮询下一路；完成选通、读取、剔除、滤波、超时标记。 |
| `getZoneState(Direction direction) -> ZoneState` | 方向 | 输出 `distanceMm / valid / lastValidMs / risk`。 |

A 的验收重点：单传感器稳定、三通道方向映射正确、断开任一传感器不阻塞、500ms 无有效更新标记 invalid、静止读数不明显乱跳、快速靠近能在 0.5s 内升档。

### 3. 决策模块

负责人：C。

目标：把三个 `ZoneState` 变成一个明确的反馈决策。决策逻辑保持纯函数，优先在电脑端测试，再接入硬件。

| 函数 | 输入 | 输出 / 行为 |
|---|---|---|
| `classifyRiskWithHysteresis(RiskLevel previousRisk, uint16_t distanceMm)` | 历史风险、当前距离 | 输出最终风险档；靠近时立即升级，远离时使用 50mm 滞回。 |
| `buildSystemState(ZoneState zones[3]) -> SystemState` | 三方向状态 | 汇总三方向数据，生成单传感器异常和全传感器异常标志。 |
| `isNearestObstacle(ZoneState candidate, ZoneState zones[3]) -> bool` | 候选方向、三方向状态 | 判断候选方向是否为有效且非 safe 的最近障碍。 |
| `isWideObstacle(ZoneState left, ZoneState right) -> bool` | 左右状态 | 左右均有效、均需提醒且距离差不超过 120mm 时返回真。 |
| `decideFeedback(SystemState state) -> FeedbackDecision` | 系统状态 | 选择最近障碍方向和统一风险等级；大范围障碍时同时激活左右；全部无效时不输出障碍震动。 |

C 的验收重点：400/800/1200mm 边界正确；阈值附近 20 秒不频繁跳档；左 900、中 500、右 700 时选中路；左右接近时双侧同震；无效方向不参与决策。

### 4. 震动反馈模块

负责人：B。

目标：用 MOSFET 安全驱动左、中、右三个电机，并非阻塞输出三档节奏。

| 函数 | 输入 | 输出 / 行为 |
|---|---|---|
| `feedbackInit()` | 无 | 配置三路电机 GPIO，初始全部关闭。 |
| `motorWrite(Direction direction, bool on)` | 方向、开关状态 | 写对应电机引脚，保证左/中/右映射固定。 |
| `feedbackStopAll()` | 无 | 立即关闭三路电机。 |
| `feedbackUpdate(FeedbackDecision decision, uint32_t nowMs)` | 反馈决策、当前时间 | 按风险等级更新三路输出，不允许阻塞 delay。 |
| `patternTick(FeedbackDecision decision, uint32_t nowMs)` | 决策、时间 | 生成 notice/warn/danger 对应开关时序。 |
| `feedbackSafetyCheck(uint32_t nowMs)` | 当前时间 | 检测反馈更新超时或主循环异常，必要时强制停机。 |

节奏固定为：`safe` 停止；`notice` 每 1.2s 震 100ms；`warn` 每 0.6s 震 120ms；`danger` 80ms 开 / 80ms 关，连续 6 次后停 320ms。风险升级立即切换，回到 safe 立即停止。

B 的软件验收重点：慢节奏等待期间传感器仍持续读取；danger 切 safe 立即停；三电机同时工作不使 ESP32-S3 复位；盲测三档节奏和三方向识别率不低于 90%。

### 5. 状态与调试模块

负责人：C。

目标：让每次问题都能从日志定位到感知、决策或反馈，不靠现场猜。

| 函数 | 输入 | 输出 / 行为 |
|---|---|---|
| `statusInit()` | 无 | 初始化状态 LED，默认不误导用户。 |
| `ledUpdate(SystemState state, uint32_t nowMs)` | 系统状态、时间 | 正常慢闪；任一传感器异常或系统异常快闪。 |
| `logSnapshot(SystemState state, FeedbackDecision decision, uint32_t nowMs)` | 系统状态、反馈决策、时间 | 每 250ms 输出时间、三方向距离和 valid、风险、最近方向、电机状态、异常标志。 |
| `formatZoneLog(ZoneState zone) -> String` | 单方向状态 | 格式化左/中/右单方向诊断字段。 |

日志字段保持稳定，便于复制到表格分析；异常连续输出不能影响传感器调度。

### 6. 电源、驱动与佩戴任务

负责人：B。

这是硬件主线，不做 P1/P2 扩展。

- 电源：USB 电源 bank 双输出，一路供 ESP32-S3，一路供电机轨；所有供电必须共地。
- 电机驱动：每路包含 N 沟道 MOSFET、100Ω 栅极电阻、10kΩ 下拉电阻、续流二极管；禁止 GPIO 直驱。
- 滤波：电机电源入口放 470uF 电解电容和 100nF 陶瓷电容，I2C 附近加高频滤波。
- 装配：用项目盒、魔术贴、扎带和现成腰带固定；重点保护传感器角度和线材。
- 验收：三电机同时启动主控不复位；启动复位阶段电机默认关闭；单人 1 分钟穿好；慢走、转身、轻微弯腰不错位。

### 7. 主循环集成

负责人：C 统筹，A/B 联调。

| 函数 | 职责 |
|---|---|
| `setup()` | 先关闭电机，再初始化串口、LED、感知、反馈和调试模块。 |
| `loop()` | 不使用长阻塞 delay；按时间片依次执行 `perceptionUpdate`、`buildSystemState`、`decideFeedback`、`feedbackUpdate`、`ledUpdate`、`logSnapshot`、`feedbackSafetyCheck`。 |
| `dueEveryMs(uint32_t nowMs, uint32_t &lastMs, uint32_t period)` | 统一管理 45ms 感知调度、250ms 日志等周期任务。 |

主程序只负责调度和数据流，不写具体滤波、分级或节奏细节。

## 建议开发顺序

1. C 建公共类型、配置和决策纯逻辑，并先完成电脑端边界测试。
2. A 单个 VL53L1X 直连测通，再接 TCA9548A 单通道，最后三通道固定映射。
3. B 单路 MOSFET 电机驱动测通，再扩三路，同时完成电源和佩戴第一版。
4. C 接入状态 LED 和串口日志，先让日志能完整显示三方向状态。
5. 三人联调：感知 → 决策 → 反馈闭环，完成方向映射和节奏验收。
6. 最后做佩戴行走、30 分钟连续运行和 10 次路线演示。

## Test Plan

- 逻辑测试：风险边界、50mm 滞回、最近障碍优先、左右大范围障碍、无效方向剔除、全部传感器无效。
- 感知测试：单传感器、三通道映射、断开一路、恢复一路、静止滤波、手掌快速靠近。
- 反馈测试：三档节奏、方向映射、安全停机、风险升级立即切换、三电机同时工作。
- 整机测试：方向盲测、节奏盲测、5 类真实物体、佩戴稳定、连续 30 分钟、完整路线 10 次。
- 所有结果记录到 `project/02-TEST_RECORD.md`，误报和漏报也必须记录。

## Assumptions

- 只做 P0，不做跌倒检测、语音播报、电量显示和演示模式。
- 目标平台为 ESP32-S3 Arduino 工程。
- 感知模块输出即时风险，决策模块输出带滞回的最终风险；电机动作只听决策模块。
- 函数名是建议命名，团队可以统一调整，但模块间输入输出契约不变。
